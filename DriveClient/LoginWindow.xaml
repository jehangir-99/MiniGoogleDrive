<Window x:Class="DriveClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mini Google Drive"
        Height="520"
        Width="920"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#ECEFF4">

    <Window.Resources>

        <!-- MODERN BUTTON STYLE -->
        <Style TargetType="Button">

            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="Opacity"
                                        Value="0.90"/>
                            </Trigger>

                            <!-- CLICK -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                        Property="Opacity"
                                        Value="0.75"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>

        <!-- MODERN TEXTBOX STYLE -->
        <Style TargetType="TextBox">

            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="TextBox">

                        <Border x:Name="border"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">

                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="2"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- FOCUS EFFECT -->
                            <Trigger Property="IsKeyboardFocused"
                                     Value="True">

                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="#4F46E5"/>

                                <Setter TargetName="border"
                                        Property="BorderThickness"
                                        Value="2"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>

    </Window.Resources>

    <Grid>

        <!-- MAIN CARD -->
        <Border Width="800"
                Height="440"
                Background="White"
                CornerRadius="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT SIDE -->
                <Border Background="#4F46E5"
                        CornerRadius="20,0,0,20">

                    <Grid>

                        <StackPanel VerticalAlignment="Center"
                                    Margin="35">

                            <TextBlock Text="☁"
                                       FontSize="60"
                                       Foreground="White"
                                       HorizontalAlignment="Center"/>

                            <TextBlock Text="Google Drive"
                                       FontSize="30"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       TextAlignment="Center"
                                       Margin="0,15,0,0"/>

                            <TextBlock Text="Secure Cloud Storage System"
                                       Margin="0,15,0,0"
                                       FontSize="16"
                                       Foreground="#D1D5DB"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"/>

                            <TextBlock Text="Upload • Download • Sync"
                                       Margin="0,30,0,0"
                                       FontSize="14"
                                       Foreground="#E5E7EB"
                                       TextAlignment="Center"/>

                        </StackPanel>

                    </Grid>

                </Border>

                <!-- RIGHT SIDE -->
                <Grid Grid.Column="1"
                      Margin="55">

                    <StackPanel VerticalAlignment="Center">

                        <!-- TITLE -->
                        <TextBlock Text="Welcome Back"
                                   FontSize="30"
                                   FontWeight="Bold"
                                   Foreground="#111827"/>

                        <TextBlock Text="Login or create a new account"
                                   Margin="0,6,0,30"
                                   Foreground="#6B7280"
                                   FontSize="14"/>

                        <!-- USERNAME -->
                        <TextBlock Text="Username"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"/>

                        <TextBox x:Name="UsernameBox"/>

                        <!-- PASSWORD -->
                        <TextBlock Text="Password"
                                   Margin="0,12,0,0"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"/>

                        <Grid Margin="0,10">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="55"/>
                            </Grid.ColumnDefinitions>

                            <!-- PASSWORD BOX -->
                            <PasswordBox x:Name="PasswordBox"
                                         Height="42"
                                         Padding="10"
                                         FontSize="14"
                                         Background="#F9FAFB"
                                         BorderBrush="#D1D5DB"
                                         BorderThickness="1"/>

                            <!-- SHOW PASSWORD BUTTON -->
                            <Button Grid.Column="1"
                                    Content="👁"
                                    Background="#6B7280"
                                    Margin="5,0,0,0"
                                    Click="ShowPassword_Click"/>

                        </Grid>

                        <!-- LOGIN BUTTON -->
                        <Button Content="Login"
                                Background="#4F46E5"
                                Margin="0,25,0,0"
                                Click="Login_Click"/>

                        <!-- REGISTER BUTTON -->
                        <TextBlock Text="Don't have an account? Create one"
                                Foreground="#4F46E5"
                                Cursor="Hand"
                                HorizontalAlignment="Center"
                                Margin="0,15,0,0"
                                MouseDown="GoToRegister_Click"/>

                        <!-- STATUS -->
                        <TextBlock x:Name="StatusText"
                                   Margin="0,25,0,0"
                                   Foreground="#6B7280"
                                   HorizontalAlignment="Center"
                                   FontSize="13"/>

                    </StackPanel>

                </Grid>

            </Grid>

        </Border>

    </Grid>
</Window>